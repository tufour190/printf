#include "main.h"

/**
 * _strlen - calculates the length of a string
 * @s: the string whose length is be determined
 * Return: the length
 */

int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)
		;
	return (0);
}
